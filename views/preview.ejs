<!DOCTYPE html>
<html lang="vi" data-theme="light">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Xem Trước Link</title>
    <link rel="icon" type="image/png" href="https://i.ibb.co/ZKvJqzL/favicon-2.png">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <style>
        /* Copy các biến CSS từ index.ejs */
        /* ... Thêm các biến CSS giống như trong error.ejs ... */

        body {
            background: var(--bg-color);
            color: var(--text-color);
            min-height: 100vh;
            display: flex;
            flex-direction: column;
        }

        .card {
            background: var(--card-bg);
            border: none;
            border-radius: 1rem;
            box-shadow: 0 4px 6px -1px var(--shadow-color);
        }

        .footer {
            margin-top: auto;
            text-align: center;
            padding: 2rem;
            color: var(--text-muted);
            font-size: 0.9rem;
        }

        #loading {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: var(--bg-color);
            display: flex;
            justify-content: center;
            align-items: center;
            z-index: 9999;
        }

        #loading img {
            width: 100px;
            height: 100px;
        }

        /* ... Copy các styles khác từ error.ejs ... */
    </style>
</head>
<body>
    <div id="loading">
        <img src="https://files.catbox.moe/vykfxc.gif" alt="Loading...">
    </div>

    <div class="container mt-5">
        <div class="card">
            <div class="card-body text-center">
                <h3>Xem Trước Link</h3>
                <p class="text-muted">Link này sẽ đưa bạn đến:</p>
                <div class="alert alert-info">
                    <%= url %>
                </div>
                <a href="<%= url %>" class="btn btn-primary">Tiếp Tục</a>
                <a href="/" class="btn btn-outline-secondary ms-2">Quay Lại</a>
            </div>
        </div>
    </div>

    <div class="footer">
        Made with <span class="heart">❤️</span> by 
        <a href="https://github.com/NoXiHaa" target="_blank">
            <i class="fab fa-github me-1"></i>Ngo Xuan Hoang
        </a>
    </div>

    <script>
        const LOADING_DURATION = 3000;

        window.addEventListener('load', function() {
            setTimeout(function() {
                const loading = document.getElementById('loading');
                loading.style.opacity = '0';
                loading.style.transition = 'opacity 0.5s';
                
                setTimeout(() => {
                    loading.style.display = 'none';
                }, 500);
            }, LOADING_DURATION);
        });
    </script>
</body>
</html>